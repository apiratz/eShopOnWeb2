apiVersion: v1
kind: Namespace
metadata:
  name: eshop-012
  
---

apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: eshop-012
  name: webapp
  labels:
    app: sample-app
spec:
  replicas: 1
  selector:
    matchLabels:
      service: webapp
  template:
    metadata:
      labels:
        app: sample-app
        service: webapp
    spec:
      containers:
        - name: webapp
          image: aistraining.azurecr.io/eshoponwebapp-012:1.0.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              protocol: TCP
          env:
            - name: ASPNETCORE_URLS
              value: http://+:80
            - name: WebApiBaseAddress
              value: http://webapi